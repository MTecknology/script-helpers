#!/bin/sh
##
# Run tests for detect_distro().
##
#set -x

. "$TEST_DIR/../lib/shell"


# Check that some distro is detected
( detect_distro; test -n "$H_DISTRO" ); [ "$?" = 0 ] && exit 0


# Complete
exit 0
